# Frontend

This folder is the real renderer for the push homepage.

Current scope:

- one page only
- 2x3 push card grid
- bottom detail panel
- one refresh button
- no mock data
- no hot/search/settings implementation

The renderer expects the Electron preload bridge from `desktop/preload.js`.
